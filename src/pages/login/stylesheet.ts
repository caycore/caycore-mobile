import {
    StyleSheet
} from "react-native";

export default StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
        flex: 1
    },
    headerContainer: {
        justifyContent: "center",
        alignItems: "center"
    },
    title: {
        textAlign: "center"
    },
    welcomeText: {
        textAlign: "center"
    },
    input: {
    }
});
